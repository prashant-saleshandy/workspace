{
  "servers": {
    "grafana": {
      "type": "stdio",
      "description": "Saleshandy Grafana observability MCP. Enable explicitly only when you need dashboards, metrics, Prometheus queries, or alerts.",
      "command": "uvx",
      "args": ["mcp-grafana"],
      "env": {
        "GRAFANA_URL": "$GRAFANA_URL",
        "GRAFANA_SERVICE_ACCOUNT_TOKEN": "$GRAFANA_SERVICE_ACCOUNT_TOKEN"
      },
      "default": "disabled"
    }
  }
}
